# hyprpaper >= 0.8 config format.
# The old `preload =` key is gone and `wallpaper` is now a block,
# so the legacy one-liner syntax silently disabled every wallpaper.
splash = 0

wallpaper {
    monitor =
    path = /home/peet/wallpaper/DesktopBG.jpeg
    fit_mode = cover
}
